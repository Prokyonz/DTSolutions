<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DiamondTradeApp.Views.NewItemPage"
             Shell.PresentationMode="ModalAnimated"
             Title="Change Password"             
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true">
    <StackLayout>
        <ScrollView>
            <StackLayout>
               
                <StackLayout Padding="40" Spacing="20">
                    <Entry x:Name="txtOldPassword" PlaceholderColor="DarkBlue" Placeholder="old password" HeightRequest="60"   />
                    <Entry x:Name="txtNewPassword" Placeholder="new password" HeightRequest="60"  />
                    <Entry x:Name="txtConfPassword" Placeholder="confirm password" HeightRequest="60"  />
                    <Button Text="Change Password" HeightRequest="50" BackgroundColor="DarkBlue" TextColor="White" CornerRadius="5" />
                </StackLayout>
                
            </StackLayout>

        </ScrollView>


    </StackLayout>
</ContentPage>